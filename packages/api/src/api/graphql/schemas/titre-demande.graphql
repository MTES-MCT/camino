# import * from 'titres.graphql'

type TitreDemandeIds {
  titreId: ID!
  titreEtapeId: ID!
}

input InputTitreDemande {
  nom: String!
  entrepriseId: ID!
  typeId: ID!
  references: [InputReference]
  titreFromIds: [ID]
}
