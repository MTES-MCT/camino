# import * from 'scalars.graphql'
# import * from 'titres.graphql'

type Activites {
  elements: [Activite]
  page: Int
  intervalle: Int
  ordre: String
  colonne: String
  total: Int
}

type Activite {
  id: ID!
  slug: String
  titre: Titre
  typeId: String!
  activiteStatutId: String!
  date: String!
  dateSaisie: String
  periodeId: Int
  annee: Int
  sections: Json
  contenu: Json
}
