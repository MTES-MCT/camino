# import * from 'scalars.graphql'
# import * from 'territoires.graphql'
# import * from 'metas.graphql'

type ActiviteStatut {
  id: ID!
  nom: String!
  couleur: Couleur!
}

type ActiviteType {
  id: ID!
  nom: String!
  description: String
  dateDebut: String!
  delaiMois: Int!
  frequenceId: ID!
  sections: Json
}
